apiVersion: apps/v1
kind: Deployment
metadata:
  name: management-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: management-server
  template:
    metadata:
      labels:
        app: management-server
    spec:
      containers:
        - name: server
          image: aeron-management-server:local
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8081
              name: grpc
          env:
            - name: AERON_MANAGEMENT_SERVER_GRPC_PORT
              value: "8081"
---
apiVersion: v1
kind: Service
metadata:
  name: management-server
spec:
  type: NodePort
  selector:
    app: management-server
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 8081
      targetPort: 8081
      name: grpc
