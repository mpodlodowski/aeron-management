apiVersion: v1
kind: Pod
metadata:
  name: node-1
  labels:
    app: aeron-cluster
    node-id: "1"
spec:
  hostname: node-1
  subdomain: aeron-cluster
  restartPolicy: Always
  containers:
    - name: cluster-node
      image: aeron-cluster-demo:local
      env:
        - name: NODE_ID
          value: "1"
        - name: CLUSTER_ADDRESSES
          value: "node-0.aeron-cluster,node-1.aeron-cluster,node-2.aeron-cluster"
      volumeMounts:
        - name: shm
          mountPath: /dev/shm
        - name: data
          mountPath: /home/aeron/aeron-cluster
    - name: agent
      image: aeron-management-agent:local
      env:
        - name: AERON_MANAGEMENT_AGENT_SERVER_HOST
          value: "management-server"
        - name: AERON_MANAGEMENT_AGENT_SERVER_PORT
          value: "8081"
        - name: AERON_MANAGEMENT_AGENT_CLUSTER_DIR
          value: "/home/aeron/aeron-cluster/aeron-cluster-1/cluster"
        - name: AERON_MANAGEMENT_AGENT_ID
          value: "agent-1"
      volumeMounts:
        - name: shm
          mountPath: /dev/shm
        - name: data
          mountPath: /home/aeron/aeron-cluster
  volumes:
    - name: shm
      emptyDir:
        medium: Memory
        sizeLimit: 1Gi
    - name: data
      emptyDir: {}
