apiVersion: v1
kind: Pod
metadata:
  name: node-2
  labels:
    app: aeron-cluster
    node-id: "2"
spec:
  hostname: node-2
  subdomain: aeron-cluster
  restartPolicy: Always
  containers:
    - name: cluster-node
      image: aeron-cluster-demo:local
      env:
        - name: NODE_ID
          value: "2"
        - name: CLUSTER_ADDRESSES
          value: "node-0.aeron-cluster,node-1.aeron-cluster,node-2.aeron-cluster"
      volumeMounts:
        - name: shm
          mountPath: /dev/shm
        - name: data
          mountPath: /home/aeron/aeron-cluster
    - name: agent
      image: aeron-management-agent:local
      env:
        - name: AERON_MANAGEMENT_AGENT_SERVER_HOST
          value: "management-server"
      volumeMounts:
        - name: shm
          mountPath: /dev/shm
        - name: data
          mountPath: /home/aeron/aeron-cluster
  volumes:
    - name: shm
      emptyDir:
        medium: Memory
        sizeLimit: 1Gi
    - name: data
      emptyDir: {}
